# S25 - db: remove filetrees table/schema; canvas-doc fully owns filetree state

## Plan
- use canvas-doc filetree element data as source of truth for persisted filetree `path`
- cut frontend and API callers off `filetrees` before dropping DB schema
- remove `packages/api-filetree` after create/update/remove flows work via canvas-doc only
- then remove DB table, DB service surface, and legacy migration references

## Progress
- [x] checked current runtime flow: filetree create still inserts DB row in `filetrees`
- [x] verified canvas-doc already has `TFiletreeData.path` in `packages/service-automerge/src/types/canvas-doc.ts`
- [x] verified DB-only filetree fields `title`, `locked`, `glob_pattern` are not needed as persisted filetree metadata target for this task
- [x] built removal/cleanup file list for next implementation pass
- [x] moved create/update/remove filetree flow to canvas-doc-only path persistence
- [x] removed DB and API filetree leftovers, including `packages/api-filetree`
- [~] updated tests/mocks and generated drop-table migration
- [ ] final verification after review

Current implementation step:
- review changed files, then run broader verification if needed

## TODOS
- [x] verify canvas-doc already has filetree `path` field
- [x] define replacement create/update/remove flow for filetree widgets against canvas-doc, not `context.db.fileTree`
- [x] update hosted widget create path to stop calling `safeClient.api.filetree.create(...)`
- [x] update filetree widget logic to stop reading `canvasResult.fileTrees` from `api.canvas.get(...)`
- [x] update filetree widget logic to stop calling `safeClient.api.filetree.update(...)` for path changes
- [x] update remove/close flow to delete filetree from canvas-doc without `api.filetree.remove(...)`
- [x] remove `fileTrees` from canvas API response/contracts/types once callers are migrated
- [x] remove `packages/api-filetree` and all app wiring/imports that register or expose it
- [x] remove DB service `fileTree` CRUD surface from `packages/service-db`
- [x] remove `filetrees` from `packages/service-db/src/schema.ts`
- [x] add migration to drop `filetrees` table and update legacy migration bootstrap logic that still references `filetrees`
- [x] remove frontend/shared types that infer filetree rows from `service-db/schema`
- [x] update tests/mocks that still assume DB-backed `fileTrees` and `api.filetree.*`
- [ ] verify behavior: create filetree, reload canvas, update path, remove widget

## NOTES
User request: filetree should be completely managed by canvas-doc.
DB should not own filetree state anymore.

Current dependency map found during check:
- create path still goes through `packages/canvas/src/plugins/HostedSolidWidget/HostedSolidWidget.plugin.tsx` -> `safeClient.api.filetree.create(...)`
- filetree widget state still loads from `packages/canvas/src/components/filetree/createFiletreeContextLogic.ts` via `api.canvas.get(...).fileTrees`
- filetree widget updates still call `safeClient.api.filetree.update(...)`
- filetree close/remove still calls `packages/canvas/src/components/filetree/FiletreeHostedWidget.tsx` -> `safeClient.api.filetree.remove(...)`
- canvas API still exposes `fileTrees` in `packages/api-canvas/src/api.get-canvas.ts` and `packages/api-canvas/src/contract.ts`
- DB service still owns `fileTree` CRUD and `getFullCanvas().fileTrees`
- `packages/api-filetree` is fully DB-backed and must be removed after callers migrate

Important finding:
- `packages/service-automerge/src/types/canvas-doc.ts` already defines `TFiletreeData.path`
- so this task is mostly removal and rewiring, not new schema design inside canvas-doc

Unused-for-target-state DB fields found during grep:
- `title`
- `locked`
- `glob_pattern`

Do not start by only deleting schema.
That breaks callers still expecting DB rows and `api-filetree` routes.

## Files to change/remove next

### canvas-doc / widget creation path
- `packages/canvas/src/plugins/HostedSolidWidget/HostedSolidWidget.plugin.tsx`
- `packages/canvas/src/plugins/HostedSolidWidget/HostedSolidWidget.helpers.ts`
- `packages/api-filetree/src/api.create-filetree.ts`

### filetree widget read/update/remove path
- `packages/canvas/src/components/filetree/createFiletreeContextLogic.ts`
- `packages/canvas/src/components/filetree/FiletreeWidget.tsx`
- `packages/canvas/src/components/filetree/FiletreeHostedWidget.tsx`
- `packages/canvas/src/services/canvas/interface.ts`

### canvas API contracts still exposing DB filetrees
- `packages/api-canvas/src/api.get-canvas.ts`
- `packages/api-canvas/src/contract.ts`

### delete filetree API package after callers migrate
- `packages/api-filetree/src/contract.ts`
- `packages/api-filetree/src/api.create-filetree.ts`
- `packages/api-filetree/src/api.update-filetree.ts`
- `packages/api-filetree/src/api.remove-filetree.ts`
- `packages/api-filetree/src/handlers.ts`
- `packages/api-filetree/src/orpc.ts`
- package wiring/import sites that register `api-filetree`

### service-db cleanup
- `packages/service-db/src/schema.ts`
- `packages/service-db/src/IDbService.ts`
- `packages/service-db/src/DbServiceBunSqlite/index.ts`
- `packages/service-db/src/DbServiceBunSqlite/fx.get-full-canvas.ts`
- `packages/service-db/src/DbServiceBunSqlite/tx.create-file-tree.ts`
- `packages/service-db/src/DbServiceBunSqlite/tx.update-file-tree.ts`
- `packages/service-db/src/DbServiceBunSqlite/tx.delete-file-tree.ts`
- `packages/service-db/src/core/fx.migrations.ts`
- `packages/service-db/database-migrations/*` for drop-table migration

### tests/mocks to update
- `packages/canvas/tests/plugins/hosted-widget/HostedSolidWidgetPlugin.test.ts`
- any mocks returning `fileTrees` from canvas API
- any mocks exposing `api.filetree.*`

### LOGS
- task added from user request
- checked current flow: creating filetree still inserts DB row in `filetrees`
- expanded task with dependency-aware removal plan so work can happen in safe order
- verified canvas-doc already has `TFiletreeData.path`; no new canvas-doc field needed for `path`
- verified `title`, `locked`, and `glob_pattern` are DB-only leftovers for filetree and can be removed with DB-backed flow
- listed next files to change/remove before deleting schema
- rewired filetree widget path persistence to update canvas-doc element data instead of DB rows
- removed `packages/api-filetree` and ORPC wiring/imports
- removed DB `filetrees` schema/service usage and generated migration `0010_swift_adam_destine.sql`
- updated hosted-widget filetree tests/mocks to stop assuming backend filetree CRUD routes

---

# S13 - canvas plugins: folder-per-plugin refactor plan

## Plan
- Define the target plugin folder structure before moving files.
- Separate package-wide shared helpers/contracts from plugin-local helpers/contracts.
- Keep each plugin entry centered on one class and move operational logic into pure helper functions.
- Standardize helper signatures so side effects stay in the first argument and the serializable payload stays in the second argument.
- Sequence the refactor to preserve exports, tests, and plugin installation order while shrinking large plugin classes.

## TODOS
- [x] Audit the current `packages/canvas/src/plugins` root and classify each file as plugin entry, shared helper, shared contract, or plugin-specific helper.
- [x] Define the target folder convention for every plugin: `PluginName/PluginName.plugin.ts`, local `types.ts`, and named helper files instead of generic `utils.ts`.
- [x] Define package-level shared files for cross-plugin contracts and helpers, including a global plugin `types.ts` / `interface.ts` boundary and a shared helper area for files like `clone-drag.ts`, `node-space.ts`, `render-order.shared.ts`, and `hosted-widget-focus.shared.ts` where appropriate.
- [x] Define a pure-function helper rule: first arg is side-effectful runtime dependencies, second arg is a serializable payload; document naming for `...Fx` helpers when a helper performs side effects.
- [x] Split the refactor into safe phases: shared contracts first, plugin folder moves second, helper extraction third, index/export cleanup fourth, tests/spec updates last.
- [x] Identify the highest-churn plugin entries to shrink first: `Group`, `Shape1d`, `Shape2d`, `Text`, `HostedSolidWidget`, `SceneHydrator`, `Select`, and `Transform`.
- [x] Define acceptance checks for the implementation pass: stable public exports from `packages/canvas/src/plugins/index.ts`, unchanged `defaultPlugins()` order, and existing canvas plugin tests still covering behavior after the move.
- [ ] Shrink `packages/canvas/src/plugins/Group/Group.plugin.ts` below 500 lines by extracting static methods and related pure helpers into sibling files.
- [ ] Shrink `packages/canvas/src/plugins/Image/Image.plugin.ts` below 500 lines by extracting static methods and related pure helpers into sibling files.
- [ ] Shrink `packages/canvas/src/plugins/Pen/Pen.plugin.ts` below 500 lines by extracting static methods and related pure helpers into sibling files.
- [ ] Shrink `packages/canvas/src/plugins/Recorder/Recorder.plugin.ts` below 500 lines by extracting static methods and related pure helpers into sibling files.
- [ ] Shrink `packages/canvas/src/plugins/Shape1d/Shape1d.plugin.ts` below 500 lines by extracting static methods and related pure helpers into sibling files.
- [ ] Shrink `packages/canvas/src/plugins/Shape2d/Shape2d.plugin.ts` below 500 lines by extracting static methods and related pure helpers into sibling files.
- [ ] Shrink `packages/canvas/src/plugins/Text/Text.plugin.ts` below 500 lines by extracting static methods and related pure helpers into sibling files.
- [ ] Shrink `packages/canvas/src/plugins/HostedSolidWidget/HostedSolidWidget.plugin.tsx` below 500 lines by extracting static methods and related pure helpers into sibling files.
- [ ] Shrink `packages/canvas/src/plugins/IframeBrowserWidget/IframeBrowserWidget.plugin.tsx` below 500 lines by extracting static methods and related pure helpers into sibling files.

## NOTES
- Category: subtraction.
- Source request: refactor `packages/canvas/src/plugins` so each plugin gets its own subfolder, cross-plugin functionality moves into shared files, and plugin classes become smaller shells over helper functions.
- The canvas spec says plugins are the main feature boundary; the live plugin contract is now `packages/canvas/src/plugins/shared/interface.ts`, re-exported through `packages/canvas/src/plugins/index.ts`.
- The plugins area now uses a folder-per-plugin layout plus `packages/canvas/src/plugins/shared/`; the old flat root plugin/helper files were removed after imports were migrated.
- The desired direction is more local structure, not more indirection: shared contracts/helpers only when reused; otherwise keep files inside the owning plugin folder.
- Each plugin folder should export one main class file and colocate supporting types/helpers under explicit names such as `drag-clone.ts`, `selection-path.ts`, `shape1d-points.ts`, or `hydration-cleanup.ts` instead of generic `utils.ts`.
- Helper design rule from request: prefer pure functions; pass stateful references and imperative dependencies in argument 1, and keep argument 2 serializable.
- This refactor likely touches the spec file index once implemented because it currently documents flat plugin files.
- Implemented the first shared extraction before any plugin moves by introducing `packages/canvas/src/plugins/shared/hosted-widget.shared.ts` for the shared hosted-widget geometry/order/pointer helpers and shared hosted-widget attribute constants.
- The shared plugin contract/helper area now lives in `packages/canvas/src/plugins/shared/`, and the old root compatibility stubs for `interface.ts`, `clone-drag.ts`, `node-space.ts`, `pen.math.ts`, `render-order.shared.ts`, and `hosted-widget-focus.shared.ts` have been removed.
- Actual plugin implementations now live under `packages/canvas/src/plugins/<PluginName>/<PluginName>.plugin.ts[x]`, and callers now import via `packages/canvas/src/plugins/index.ts` or the real folder/shared paths instead of the deleted flat shims.
- `packages/canvas/src/plugins/index.ts` now exports from the new implementation folders, and `defaultPlugins()` order in `Canvas.service.ts` stayed unchanged.
- Updated `docs/specs/spec.canvas.md` so the spec now describes the shared contract boundary and folder-per-plugin implementation layout.
- Verification pass: `bun --filter @vibecanvas/canvas test` passed after the refactor.
- Full repository verification also passed through `bun run test` from the repo root.
- Remaining oversized plugin implementation files after the folder move / cleanup pass: `Group` (677), `Image` (809), `Pen` (618), `Recorder` (514), `Shape1d` (1261), `Shape2d` (838), `Text` (713), `HostedSolidWidget` (1032), and `IframeBrowserWidget` (1059).
- Next phase requirement: each plugin implementation file should be reduced below 500 lines by moving static methods and other pure helper logic into explicit sibling files inside the same plugin folder.

### LOGS
- Read `docs/specs/spec.canvas.md` to ground the plan in the current plugin architecture and package design rules.
- Audited the current `packages/canvas/src/plugins` directory and confirmed the flat root currently mixes plugin classes, shared helpers, shared contracts, and experimental/reference plugins.
- Added this subtraction entry to track the plugin-folder refactor as a dedicated planning item.
- Extracted the first shareable hosted-widget helpers into `packages/canvas/src/plugins/shared/hosted-widget.shared.ts` and updated `HostedSolidWidget`, `IframeBrowserWidget`, and `Transform` to use the shared constants/helpers before any plugin-folder moves.
- Ran focused canvas tests during the first shared-helper phase: hosted widget, iframe browser, and transform plugin coverage passed after fixing the hosted-widget transformer constant migration.
- Moved every plugin implementation into its own folder and moved shared helpers/contracts into `packages/canvas/src/plugins/shared/`; the old flat entry files were kept only temporarily during the migration and were removed in the cleanup pass.
- Updated the canvas spec file index and extension guidance to point at the new plugin folders and shared contract/helper area.
- Ran `bun --filter @vibecanvas/canvas test` and confirmed the canvas package test suite passed after the refactor.
- Ran `bun run test` from the repository root and confirmed the combined core, server, and canvas suites passed after the refactor.
- Removed the remaining root `packages/canvas/src/plugins/*.plugin.ts[x]` and shared-helper compatibility stub files, then updated repo imports to use `packages/canvas/src/plugins/index.ts` or `packages/canvas/src/plugins/shared/*` directly.
- Re-ran `bun --filter @vibecanvas/canvas test` after the cleanup pass to confirm the canvas package still passes without the flat shim files.
- Re-ran `bun run test` from the repository root after deleting the flat shim files and confirmed the full suite still passes.
- Counted plugin implementation file sizes after the cleanup pass and captured follow-up TODOs for every plugin file still above 500 lines.

---

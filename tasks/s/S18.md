# S18 - cli server: migrate http file/static/spa serving from apps/server

## Plan
- Move the remaining plain HTTP serving behavior out of `apps/server/src/server.ts` and into `apps/cli` server/runtime wiring.
- Keep websocket protocol plugins separate; this task is only about non-WS HTTP responses.

## TODOS
- [x] Serve persisted file blobs for `/files/*` in `apps/cli`.
- [x] Serve static assets from the frontend build output in `apps/cli`.
- [x] Support embedded assets / compiled-mode asset loading in `apps/cli`.
- [x] Restore SPA fallback behavior for unknown app routes.
- [ ] Remove the legacy HTTP-serving path from `apps/server` once parity is verified.

## NOTES
- Source behavior currently lives in `apps/server/src/server.ts`.
- Current gap in `apps/cli`: `ServerPlugin` returns a WIP text response for non-websocket requests.
- Keep ownership clean:
- `ServerPlugin` should route requests and compose behavior.
- low-level file/static asset access should stay in services or small explicit helpers.

### LOGS
- Added from the `apps/server` vs `apps/cli` parity review.
- Captures the missing non-websocket HTTP surface that still blocks removing `apps/server`.
- Implemented CLI-side non-websocket HTTP handling in `apps/cli/src/plugins/server/http.ts`.
- Wired `ServerPlugin` to serve persisted `/files/*`, static public assets, embedded assets, and SPA fallback responses.
- Added `apps/cli/tests/server.http.test.ts` covering blob serving, ETag behavior, and public asset lookup/path traversal protection.

---

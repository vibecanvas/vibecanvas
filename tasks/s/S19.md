# S19 - cli orpc: expose db events stream and remove apps/server api.db

## Plan
- Expose the DB event stream from `apps/cli` ORPC wiring using the extracted event publisher service.
- Remove the last legacy `apps/server/src/apis/api.db.ts` route once the stream is available from `apps/cli`.

## TODOS
- [ ] Add the `db` contract/handlers to `apps/cli` ORPC router wiring.
- [ ] Implement the `events` stream using `IEventPublisherService.subscribeDbEvents()`.
- [ ] Verify filetree mutations continue publishing DB events through the extracted service.
- [ ] Confirm whether any frontend/runtime consumers still depend on this stream.
- [ ] Remove legacy `apps/server/src/apis/api.db.ts` usage after parity is verified.

## NOTES
- The event publisher capability is already extracted in `packages/event-publisher-service`.
- `packages/api-filetree` already publishes DB change events through `publishDbEvent(...)`.
- Current gap is transport wiring: `apps/cli/src/plugins/orpc/router.ts` does not expose `api.db.events` yet.
- This is separate from `@vibecanvas/api-file`; file handlers are already present in the CLI ORPC router.
- Migration rule: do not change legacy `apps/server` code for this task; only add/update the new `apps/cli` and extracted-package wiring until parity is proven.

### LOGS
- Added from the `apps/server` vs `apps/cli` parity review.
- Split out as its own task because the service extraction is mostly done; only route exposure and cleanup remain.

---

# S26 - db: add filesystems table for local/remote machine identity

## Plan
- add `filesystems` table to represent a computer / machine identity
- stop assuming filesystem, files, and terminal APIs always run on same local machine
- prepare API and service layers to route through filesystem identity

## TODOS
- [ ] design `filesystems` table shape and ownership
- [ ] define how local machine identity is created and persisted
- [ ] attach filesystem/files/terminal resources to a filesystem id
- [ ] update APIs to accept/resolve filesystem id
- [ ] document multiplayer reasoning: one user's local machine is remote to another user
- [ ] add tests for local vs remote machine flows

## NOTES
Current assumption is wrong: always-local depends on which user is asking.
One local machine != another local machine.
From multiplayer view, another user's local machine is a remote machine.
Need first-class machine identity so filesystem, files, and terminal can bind to it later.

### LOGS
- task added from user request

---

# S27 - canvas CLI: remove unimplemented `render` command and help traces

## Plan
`render` is exposed in the CLI surface and help, but execution says it is not implemented. This is bad discovery and wastes agent context. Remove the command token and all user-facing traces until a real implementation exists.

Scope:
- remove `render` from routed canvas subcommands
- remove `render` from top-level alias/discovery text
- remove generic help references that imply `render` exists now
- remove or update tests/docs/prompts that tell agents to probe `render`

## TODOS
- [ ] Remove `render` from the canvas subcommand constant set and alias detection.
- [ ] Remove `render` mentions from root help and canvas help text.
- [ ] Remove `render` from command-specific help routing.
- [ ] Audit CLI docs/prompts/tests for stale `render` references and update them.
- [ ] Make unknown `render` usage fail like a normal unknown command, with a clear suggestion if needed.
- [ ] Add/adjust tests so help and parsing no longer advertise `render`.
- [ ] Close this leaf and flip the overview entry to `[x]` on merge.

## NOTES
- Evidence: `llm/reports/canvas-cli-dev-report-2026-04-07.md`
- Current bad behavior:
  - `render --help` falls back to generic canvas help
  - `render` execution says `Canvas command 'render' is not implemented yet.`
- Better to hide dead surface than advertise fake capability.

### LOGS
- agent: opened follow-up subtraction task from CLI research report to remove unimplemented `render` surface.

---

# S16 - canvas: fix broken TypeScript typings in packages/canvas

## Plan
- Re-run the canvas package typecheck and separate code-owned failures from dependency and ambient-lib noise.
- Fix the remaining `packages/canvas` app/test typing breakages by tightening local narrowing, callback signatures, and Konva/Solid types.
- Resolve the related cross-package typing error surfaced during the canvas pass, then re-run the typecheck to confirm the owned-error list is cleared.

## TODOS
- [x] Fix the tool-label indexing type mismatch in `packages/canvas/src/components/CanvasHelp/help.data.ts` so every `TTool` key used by the help UI is represented in the label map.
- [x] Fix the cleanup callback return type in `packages/canvas/src/components/terminal/TerminalWidget.tsx:33` so the registered Solid cleanup function returns `void | Promise<void>` instead of `Promise<number | void>`.
- [x] Fix the conditional rendering prop types in `packages/canvas/src/components/terminal/TerminalWidget.tsx:122` so the `Show` usage matches Solid's keyed overload expectations.
- [x] Fix the dragged-node typing in `packages/canvas/src/plugins/HostedSolidWidget/HostedSolidWidget.drag.ts:40`, `:60`, and `:76` so Konva node arrays are narrowed to the shape/group types expected by the drag helpers.
- [x] Fix the hosted-widget drag helper call typing in `packages/canvas/src/plugins/HostedSolidWidget/HostedSolidWidget.plugin.tsx:381` so the selected node list matches the helper's Konva shape/group contract.
- [x] Fix the DOM `pointerEvents` style typing in `packages/canvas/src/plugins/IframeBrowserWidget/IframeBrowserWidget.chrome.tsx:346` and `:362` so the assigned values satisfy the typed CSS property union.
- [x] Fix the Solid setter overload in `packages/canvas/src/plugins/IframeBrowserWidget/IframeBrowserWidget.plugin.tsx:320` so the update callback always resolves to `TBrowserElement` instead of a mixed generic/function union.
- [x] Fix the image element data construction in `packages/canvas/src/plugins/Image/Image.files.ts:30` so the created payload conforms to the `TElementData` discriminated union instead of attaching `url` to non-image shapes.
- [x] Fix the Konva image node config typing in `packages/canvas/src/plugins/Image/Image.node.ts:50` so the constructed `ImageConfig` includes the required image field or uses the correct pre-image config shape.
- [x] Fix the recorded Konva event typing in `packages/canvas/src/plugins/Recorder/Recorder.plugin.ts:158` so mouse/pointer event handling matches the actual emitted event object type.
- [x] Fix the `instanceof` operand typing in `packages/canvas/src/plugins/shared/clone-drag.ts:54-55` so the cloned node checks operate on object-typed values.
- [x] Fix the element-data narrowing in `packages/canvas/tests/plugins/hosted-widget/HostedSolidWidgetPlugin.test.ts:1466-1467` before reading `w` and `h` from the hosted widget test element.
- [x] Fix the browser-element test typing in `packages/canvas/tests/plugins/iframe-browser/IframeBrowserWidgetPlugin.test.ts:86`, `:124-126`, and `:134` by narrowing to the browser shape, typing the `tab` callback parameter, and using a valid typed tab id.
- [x] Fix the mocked delete-image typings in `packages/canvas/tests/plugins/image/ImagePlugin.test.ts:96`, `:145`, `:186`, `:229`, and `:265` so the mocks satisfy `TDeleteImage` and preserve the literal `{ ok: true }` result type.
- [x] Fix the Konva node narrowing in `packages/canvas/tests/plugins/selection-style-menu/SelectionStyleMenuPlugin.test.ts:151` and `:227-228` before reading `fill()` and `stroke()` from selected nodes.
- [x] Fix the element-data narrowing in `packages/canvas/tests/plugins/shape1d/Shape1dPlugin.test.ts:276` before reading `points` from the shape test fixture.
- [x] Fix the shape-data narrowing in `packages/canvas/tests/plugins/shape2d/Shape2dPlugin.static.test.ts:56-57`, `:86-87`, and `:134-142` before reading rect and ellipse dimension fields from `TElementData`.
- [x] Fix the cross-package adapter event typing in `packages/imperative-shell/src/automerge/adapters/websocket.adapter.ts:76` so the emitted event name matches `NetworkAdapterEvents` while checking the canvas workspace.

## NOTES
- Category: subtraction.
- Source request: user asked to lint `packages/canvas` and make a list of broken code typings, then add a subtraction task to fix them with each broken issue represented as a single TODO item.
- Scope: this task tracks code-owned typing failures from the `packages/canvas` typecheck pass, plus the one cross-package error surfaced while checking the canvas workspace.
- Dependency typing conflicts also exist in the current workspace, including `bun-types` vs `lib.dom`, `@types/node`, `@kobalte/core`, and duplicated `@codemirror/view` versions.
- The broader dependency cleanup is still separate, but the canvas package now typechecks by fixing owned code and adding one local `CodeEditor` keymap cast to absorb the duplicated CodeMirror keymap type identity.

### LOGS
- Ran `bunx tsc -p packages/canvas/tsconfig.json --pretty false` from the repo root to capture the current canvas lint/typecheck typing failures.
- Separated dependency and ambient declaration noise from owned app/test failures in `packages/canvas`, and kept the related cross-package websocket adapter error because it surfaced during the same canvas check.
- Created `S16` and added the overview entry in `tasks/BASED.md` to track the canvas typings cleanup pass.
- Fixed the app-side typing issues in canvas help, terminal widget cleanup/rendering, hosted widget drag contracts, iframe browser CSS/setter types, image element/node construction, recorder pointer-move typing, clone-drag narrowing, and the websocket adapter event emission.
- Fixed the test-side typing issues by adding explicit element-data narrowing/casts in the hosted widget, iframe browser, image, selection style menu, shape1d, and shape2d tests.
- Added a local `CodeEditor` keymap cast so the canvas package remains type-safe under the current duplicated `@codemirror/view` install graph without broad dependency surgery.
- Re-ran `bunx tsc -p packages/canvas/tsconfig.json --noEmit --pretty false --skipLibCheck` and confirmed the canvas workspace now passes the owned typecheck.

---

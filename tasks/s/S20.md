# S20 - cli server: restore compiled-mode port fallback when preferred port is busy

## Plan
- Bring back the compiled-mode port retry behavior from legacy `apps/server` into `apps/cli` server boot.
- Keep dev behavior strict unless there is a reason to preserve fallback there too.

## TODOS
- [x] Port the compiled-mode fallback bind loop into `apps/cli` server startup.
- [x] Keep the preferred port when it is free.
- [x] Retry on the next ports when compiled mode finds the preferred port busy.
- [x] Log clearly when the runtime had to move off the preferred port.
- [ ] Remove the duplicated legacy fallback path from `apps/server` once parity is verified.

## NOTES
- Source behavior currently lives in `apps/server/src/server.ts` via `serveWithPortFallback(...)`.
- Current `apps/cli` gap: `ServerPlugin` binds directly to `ctx.config.port` with no retry logic.
- This task is explicitly about compiled-mode parity, not general dev-server auto-port discovery.
- Migration rule: do not change legacy `apps/server` code for this task; only add/update the new `apps/cli` server wiring until parity is proven.

### LOGS
- Added from the `apps/server` vs `apps/cli` parity review.
- Tracks the remaining bind behavior needed before removing `apps/server`.
- Implemented compiled-mode fallback in `apps/cli/src/plugins/server/ServerPlugin.ts` without changing legacy `apps/server`.
- Added `apps/cli/tests/server.plugin.test.ts` covering preferred-port success, compiled fallback, and dev-mode strict behavior.
- Verified with `bun test apps/cli/tests/server.plugin.test.ts apps/cli/tests/server.http.test.ts`.

---

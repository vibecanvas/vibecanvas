# S17 - extract apps/server into apps/cli + shared packages

## Plan
Break the monolithic apps/server into a plugin-based apps/cli that composes shared packages.
apps/server stays as legacy until fully migrated.

Important architectural rule:
- plugins are wiring/lifecycle units
- services are runtime capabilities behind stable interfaces
- plugins must depend on service contracts, not concrete implementations

Service model refinement:
- a service is not a plugin and should not have `after` / `apply`
- base service concerns are small capability interfaces (`IService`, `IStartableService`, `IStoppableService`, `IEventSource<T>`)
- events are optional service capabilities, not mandatory base behavior

Example:
- `ApiPlugin` should depend on `IDbService`
- not on `SqliteDbService` directly

This keeps implementations swappable and lets future apps (e.g. `apps/saas`) reuse only the contracts + implementations they need.

Target structure:
```
apps/cli/              -- plugin-based entry point (replaces apps/server)
packages/tapable/      -- extract hook system (SyncHook, AsyncSeriesHook, etc.)
packages/runtime/      -- plugin runtime (IPlugin, service registry, createRuntime)
packages/shared-functions/ -- deep-import shared fn/fx/tx helpers without package-root barrel
packages/api/          -- business logic / service layer (ORPC contracts + handlers)
packages/db/           -- database (SQLite/Drizzle)
packages/filesystem/   -- filesystem watcher
packages/pty/          -- terminal service
packages/automerge/    -- CRDT repo
```

This enables future apps (e.g. apps/saas) to reuse parts of the stack
without pulling in the full server.

Builds on [E13](../e/E13.md) plugin system design.

## TODOS
- [x] Extract packages/tapable from packages/canvas/src/tapable + new async hooks
- [x] Extract packages/runtime for plugin/service composition
- [~] Extract packages/db from apps/server + packages/imperative-shell (package + migrations moved; caller migration and plugin wiring still remain)
- [ ] Extract packages/automerge
- [ ] Extract packages/pty
- [ ] Extract packages/filesystem
- [~] Extract packages/api (canvas + file packages scaffolded; aggregator/server wiring still pending)
- [x] Create apps/cli with plugin system skeleton (create-runtime + bootstrap helpers)
- [~] Wire apps/cli to replace apps/server entry point (bootstrap/config/services wired; command parity and plugins still missing)
- [ ] Deprecate apps/server

## NOTES

### Why
- apps/server is monolithic: DB, PTY, automerge, API, network, CLI all wired in one place
- Can't reuse pieces independently (e.g. saas needs API+DB but not PTY)
- Plugin system (E13) gives the composition model, this task does the extraction

### Dependency model

```text
apps/cli
├─ uses packages/tapable
├─ uses packages/api
├─ uses packages/db
├─ uses packages/filesystem
├─ uses packages/pty
└─ uses packages/automerge

runtime wiring

DbPlugin -------------> provides "db" : IDbService
AutomergePlugin ------> provides "repo" : IAutomergeService
FilesystemPlugin -----> provides "filesystem" : IFilesystemService
PtyPlugin ------------> provides "pty" : IPtyService
ApiPlugin ------------> requires "db" | "repo" | "filesystem" | "pty"
                     -> provides "api" : IApiService
NetworkPlugin --------> requires "api"
CliPlugin ------------> requires "api" (or narrower command services)
```

### Work completed so far
- Created `packages/tapable`.
- Added shared hook primitives there:
  - `SyncHook`
  - `SyncExitHook`
  - `AsyncParallelHook`
  - `AsyncSeriesHook`
  - `AsyncWaterfallHook`
- Created `packages/runtime`.
- Moved plugin runtime concerns there:
  - `IPlugin`
  - `IPluginContext`
  - `IServiceMap`
  - `IServiceRegistry`
  - `createServiceRegistry`
  - `createRuntime`
  - `topoSort`
- Made `IPlugin` generic over required services so plugins can express typed service dependencies.
- Made runtime hooks generic too; shared runtime no longer hardcodes CLI/server hook names.
- Created a minimal `apps/cli`:
  - `src/main.ts`
  - `src/hooks.ts`
- Updated `apps/cli` to consume `@vibecanvas/runtime` via local re-export shims.
- Moved CLI-specific hooks + lifecycle orchestration into `apps/cli/src/hooks.ts`.
- Verified the naked CLI runtime boots with zero plugins.
- Created `packages/db` with:
  - `src/IDbService.ts` (SQLite-agnostic Drizzle-first service contract)
  - `src/interface.ts` (`IDbConfig` and related non-service interfaces)
  - `src/schema.ts` (Drizzle schema + Zod select schemas)
  - `src/DbServiceBunSqlite.ts` (real Bun SQLite + Drizzle implementation class)
  - `src/core/fx.migrations.ts` (functional effect-style migration logic)
-  `src/core/fx.resolve-config.ts` (functional effect-style config resolution)
- Kept Drizzle as the known DB interface by defining `IDbService` as the typed Drizzle database over the shared schema, without leaking SQLite-specific details into the service contract.
- Introduced `DbServiceBunSqlite` as the concrete Bun SQLite implementation class.
- Removed the redundant `IDbHandle` wrapper and `IResolvedDbConfig` alias; callers now use `DbServiceBunSqlite` directly when they need implementation details.
- Refined the DB abstraction further: `IDbService` is now a minimal technology-agnostic runtime service contract, while Drizzle lives only on the concrete `DbServiceBunSqlite.drizzle` property.
- Created `packages/db/src/core/` for functional-style DB effect files and moved migrations there without introducing extra module nesting.
- Removed `packages/db/src/index.ts`; `@vibecanvas/db` now follows deep-import-only usage to avoid future export conflicts.
- Pointed DB migration resolution at `packages/db/database-migrations` so the extracted package now owns its source-tree migrations.
- Created `packages/shared-functions` as a deep-import-only package for reusable `fn.*` / `fx.*` / `tx.*` helpers outside `packages/functional-core`.
- Copied `vibecanvas-config` into `packages/shared-functions/src/vibecanvas-config/`:
  - `fn.xdg-paths.ts`
  - `tx.config-path.ts`
  - `fn.xdg-paths.test.ts`
- Inlined error codes inside the copied files instead of keeping a separate `err.codes.ts`.
- Updated DB config resolution to import `txConfigPath` from `@vibecanvas/shared-functions/vibecanvas-config/tx.config-path`.
- Refactored `apps/cli` bootstrap to prepare for command parity with legacy `apps/server/src/main.ts`:
  - `src/parse-argv.ts`
  - `src/build-config.ts`
  - `src/setup-services.ts`
  - `src/setup-signals.ts`
- Moved CLI config ownership out of `packages/runtime`; runtime is now generic over app config while `apps/cli` owns a fully resolved `ICliConfig`.
- `ICliConfig` now resolves `port`, `dbPath`, `configPath`, `dataPath`, and `cachePath` before runtime creation.
- `apps/cli/src/main.ts` now follows: parse argv -> build config -> setup services -> create runtime -> setup signals -> boot.
- Wired the DB service into `apps/cli` service setup and generic stoppable-service shutdown flow.
- Added `apps/cli/src/plugins/cli/CliPlugin.ts` for help/version/WIP command handling.
- Added `apps/cli/src/plugins/server/ServerPlugin.ts` with a dummy Bun server for the `serve` command.
- Created `packages/api-canvas` with:
  - `src/contract.ts` (copied canvas ORPC contract fragment)
  - `src/handlers.ts` (already ORPC-bound handlers)
  - `src/types.ts` (minimal API context type)
-  - one file per endpoint implementation:
   - `src/api.list-canvas.ts`
   - `src/api.get-canvas.ts`
   - `src/api.create-canvas.ts`
   - `src/api.update-canvas.ts`
   - `src/api.remove-canvas.ts`
- Switched the extracted canvas contract to use `@vibecanvas/db/schema` instead of shell schema imports.
- Replaced the initial canvas WIP implementation object with real handler-per-file wiring modeled after legacy `apps/server/src/apis/api.canvas.ts`.
- Moved canvas read/update usecases into `IDbService` (`listCanvas`, `getFullCanvas`, `updateCanvas`) and implemented them in `DbServiceBunSqlite`.
- Refactored `packages/api-canvas` so its context now depends only on `IDbService` for read/update handlers.
- Current canvas API note: `create` and `remove` remain WIP until repo/automerge capabilities are extracted behind a non-legacy service contract.
- Created `packages/api-file` with:
  - `src/contract.ts` (copied file ORPC contract fragment)
  - `src/implementation.ts` (WIP throwing implementation)
  - `src/handlers.ts` (already ORPC-bound handlers)
  - `src/types.ts` (minimal API context type)
- Added shared runtime service capability interfaces in `packages/runtime`:
  - `IService`
  - `IStartableService`
  - `IStoppableService`
  - `IManagedService`
  - `IEventSource<T>`
- Updated `IDbService` to implement the shared service model while still exposing Drizzle directly (`name`, `sqlite`, `stop()`).
- Added compatibility shims in `packages/imperative-shell/src/database/db.ts` and `schema.ts` so existing imports keep working while callers migrate.
- Current DB migration note: source-tree migrations now live under `packages/db/database-migrations`; embedded migration assets are still transitional/not yet owned by the package.

### Current shape of apps/cli

```text
apps/cli/
├── package.json
├── tsconfig.json
└── src/
    ├── build-config.ts
    ├── parse-argv.ts
    ├── setup-services.ts
    ├── setup-signals.ts
    ├── hooks.ts
    ├── main.ts
    └── plugins/
        ├── cli/
        │   ├── CliPlugin.ts
        │   └── tx.cmd.upgrade.ts
        └── server/
            └── ServerPlugin.ts
```

### Next extraction order
1. Wire `packages/api-canvas` and `packages/api-file` into `packages/core-contract` so the aggregator imports contract fragments from extracted packages
2. Wire extracted API handlers into server/app routing while leaving implementation WIP-safe
3. Migrate DB callers from imperative-shell re-export shims to direct `@vibecanvas/db/...` deep imports
4. Extract `packages/automerge`, `packages/pty`, and `packages/filesystem` with service contracts + concrete implementations
5. Add build presets for CLI/server/saas-style compositions
6. Continue moving command handling (`serve` / `canvas` / `upgrade`) toward `apps/cli` command parity with legacy `apps/server`

### API contract packaging

- ORPC client typing still needs one final static `apiContract` per app/build.
- Runtime API plugins may be composable, but client types cannot depend on runtime-discovered plugins.
- Therefore, contract composition must happen at build time.

Recommended shape:

```text
packages/api-public/
├─ src/contract.ts     -- public ORPC contract fragment
├─ src/plugin.ts       -- runtime plugin/handlers
└─ separate exports    -- frontend imports contract entrypoint only

packages/api-enterprise/
├─ src/contract.ts
├─ src/plugin.ts
└─ separate exports

packages/core-contract/ (or renamed aggregator package)
├─ compose public + enterprise fragments into final app contracts
├─ export publicApiContract
├─ export enterpriseApiContract
└─ export default/full apiContract variants as needed
```

Rules:
- contracts can live in `packages/api-public` / `packages/api-enterprise`; they do **not** need a dedicated extra package
- but contract entrypoints must stay free of server-only/runtime-only imports
- frontend should import exactly one final composed contract for its build target
- runtime plugin selection and contract composition must stay aligned

Implication for frontend:
- `apps/frontend/src/services/orpc-websocket.ts` should continue importing a single `apiContract`
- that contract should come from an app/build-specific composition, not from runtime plugin discovery

### Open decisions / reminders
- `apps/server` remains legacy during migration; avoid big-bang replacement.
- `apps/cli` is the new runtime home for the plugin system.
- `packages/tapable` should stay hook-focused; plugin ordering + service abstractions live in `packages/runtime`.
- shared runtime should stay hook-agnostic; app runtimes define their own hook bags and boot/shutdown semantics.
- Future apps should compose subsets of the stack through service contracts.
- ORPC contracts should be modular by domain/edition, but each frontend build still needs one statically composed final contract.
- `packages/db` currently preserves old callsites through imperative-shell re-export shims; next step is moving direct imports to `@vibecanvas/db` and adding a DB plugin.
- `packages/db` should remain deep-import-only; do not reintroduce a root barrel export.
- `packages/shared-functions` should also remain deep-import-only; no root barrel export.
- For `packages/shared-functions`, inline error codes in each file instead of adding per-module `err.codes.ts` files.
- `apps/cli` should keep command parsing/config assembly in dedicated bootstrap helpers instead of growing `main.ts` into a new monolith.
- `setup-services.ts` is still transitional, but DB is intentionally treated as a foundational service used by other plugins rather than a required first plugin extraction step.
- `packages/api-canvas` and `packages/api-file` currently prove package ownership/wiring only; real implementation is intentionally WIP and still needs aggregator/server integration.
- `packages/api-canvas` now uses `IDbService` for read/update logic; remaining create/remove gaps are about missing repo-service extraction, not DB coupling.
- services should use optional capability interfaces for lifecycle/events; plugin ordering and `apply()` stay in the plugin layer only.
- Route/command registries still need to be formalized once service packages land.

---

# A20 - canvas CLI: agent-friendly help, discovery, and forgiving errors

## Plan
Current canvas CLI is usable for humans with source access, but rough for AI agents probing it blind. Research from `llm/reports/canvas-cli-dev-report-2026-04-07.md` found three bad patterns:
- help can fall back to generic menus (`render --help`)
- error messages can be technically true but not discovery-friendly (`Unknown patch branch 'locked'`)
- online/no-`--db` path can briefly disagree with offline `--db` path after recent mutations

Goal:
- make the CLI forgiving for common agent mistakes
- help agents discover the surface without reading source
- keep help incremental so one command does not dump huge context

Scope:
- help and error-contract pass over the canvas CLI first
- improve discovery, suggestions, examples, and next-step hints
- do not expand into natural-language parsing

## TODOS
- [ ] Audit every canvas subcommand help path: `list`, `query`, `patch`, `move`, `group`, `ungroup`, `delete`, `reorder`, `render`, plus top-level aliases.
- [ ] Define one small help ladder: root help -> canvas help -> subcommand help -> focused error hint. Each layer should only show what is needed next.
- [ ] Add real subcommand help or explicit unimplemented help for `render`; stop falling back to generic canvas help.
- [ ] Add suggestion text for wrong commands / aliases, e.g. unknown subcommand should suggest nearest valid commands.
- [ ] Improve patch errors to explain the required envelope shape with tiny examples, e.g. `{"element":{...}}` and `{"group":{...}}`.
- [ ] Improve selector errors to say what is missing, what conflicts, and one valid minimal command to try next.
- [ ] Standardize JSON errors so every failure includes stable machine fields plus an optional `hint` or `next` field safe for agents to read.
- [ ] Decide which common mistakes should be forgiven instead of rejected, e.g. `render --help`, missing selector mode, single obvious patch branch, or alias/help misuse.
- [ ] Add a small discovery command or mode if needed, but keep output incremental. Example options: `--help`, `--examples`, `--schema`, or command-specific `--help=examples` style.
- [ ] Review local `--db` vs discovered-server behavior and document or reduce stale-result windows so help/errors do not hide path differences.
- [ ] Add tests for help routing, suggestion text, patch-shape hints, selector conflict hints, JSON error contract, and alias behavior.
- [ ] Add one blind-agent smoke test: start from help only, no source, and verify the agent can reach a valid `query` and `patch` command from CLI feedback alone.
- [ ] Close this leaf and flip the overview entry to `[x]` on merge.

## NOTES
- Main evidence so far lives in `llm/reports/canvas-cli-dev-report-2026-04-07.md`.
- Concrete issues already seen:
  - `render --help` prints generic canvas help, while `render` exec says not implemented.
  - patch help does not explain the envelope shape, so a natural payload like `{"locked":true}` fails with low-discovery output.
  - no-`--db` query can briefly show stale structure after offline `--db` mutations.
- Good CLI for agents should prefer:
  - exact error code
  - short explanation
  - one likely fix
  - one minimal example
- Keep help small. Do not dump every arg on every failure.
- Good first slice: fix help routing + patch hints + unknown-command suggestions before bigger forgiveness rules.

### LOGS
- agent: used dev CLI research from `llm/reports/canvas-cli-dev-report-2026-04-07.md`; opened follow-up task for agent-friendly help/discovery/error UX.

---

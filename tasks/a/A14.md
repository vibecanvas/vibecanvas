# A14 - canvas CLI: `delete` command

## Plan
- Add explicit destructive deletion with documented side-effect modes.
- Keep document deletion available offline while making live cleanup opt-in and best-effort.
- Report warnings whenever side effects cannot be applied.

## TODOS
- [ ] Write `packages/canvas/tests/cli/delete.test.ts` first, covering element/group deletion, root-collapsing behavior, `--doc-only`, `--with-effects-if-available`, and warning output.
- [ ] Implement `vibecanvas canvas delete` with explicit mode flags for document-only versus best-effort side-effect cleanup.
- [ ] Extract or reuse pure root-collapse/document-delete helpers from existing selection/delete logic where possible.
- [ ] Return stable JSON for deleted ids, skipped effects, and warnings.

## NOTES
- Category: addition.
- Scope decision: `delete` must expose `--doc-only` and `--with-effects-if-available` modes.
- Dependency: build after A5 and A6.

### LOGS
- Created from E8 after freezing the concrete implementation strategy.

---

# A23 - canvas CLI: document minimal required add args per element type and default optional fields

## Plan
Make `canvas add` agent-friendly by defining the smallest valid payload per element type, defaulting optional fields, and exposing that contract clearly in help/schema output.

## TODOS
- [x] Define minimal required fields for each supported add element type
- [x] Define default values for optional fields per element type
- [x] Keep add accepting compact payloads that omit optional fields
- [x] Make `--help` show required vs optional fields clearly per type
- [x] Make schema/help output show defaults, not just field names
- [x] Add examples with minimal valid payloads for each type
- [x] Add tests that compact payloads expand to correct stored defaults
- [x] Add tests that missing required fields fail with clear next-step hints

## NOTES
- Goal is better authoring UX for agents and humans using JSON
- Focus is `canvas add`
- Contract should answer: what is the minimum payload for `rect`, `ellipse`, `diamond`, `text`, `line`, `arrow`, and other supported addable types
- Optional fields should not need to be repeated when CLI/runtime can safely default them
- Help output should be explicit enough that an agent can build valid payloads without reading source code
- Good output shape: per type list of required fields, optional fields, and defaults
- Keep single-source-of-truth if possible so runtime defaults and help text do not drift

### LOGS
- Added task from user request.
- Added shared add-contract defaults/helper rendering for supported primitive types.
- Updated `canvas add --help` to print minimal JSON payloads and defaulted optional fields per type.
- Added minimal rect/text help examples.
- Updated add command runtime to accept minimal payloads and inject `portal.crypto` for fresh ids.
- Added tests for default expansion, help content, executable help examples, and missing-type failures.

---

# A12 - canvas CLI: `group` command

## Plan
- Add explicit grouping around a selected set of ids.
- Preserve absolute child positions while changing structure.
- Keep the transform compatible with current group semantics.

## TODOS
- [ ] Write `packages/canvas/tests/cli/group.test.ts` first, covering grouping explicit ids, preserving positions, and rejecting invalid target combinations.
- [ ] Implement `vibecanvas canvas group` as a deterministic document transform over explicit targets.
- [ ] Extract or reuse pure grouping helpers from the runtime/plugin path where possible.
- [ ] Return stable JSON describing the new group and member ids.

## NOTES
- Category: addition.
- Scope decision: grouping should follow existing runtime semantics instead of inventing CLI-only behavior.
- Dependency: build after A5 and A6.

### LOGS
- Created from E8 after freezing the concrete implementation strategy.

---

# A27 - theme: make canvas overlays and terminal fully theme-aware
[Overview](../BASED.md)

Finish theme wiring inside canvas DOM overlays and terminal chrome.

## Context
Shared theme service now exists in `packages/service-theme/` and basic canvas render bits already read it. Remaining canvas UI still mixes hardcoded colors and Tailwind color classes.

Relevant files:
- `packages/canvas/src/components/terminal/GhosttyTerminalMount.tsx`
- `packages/canvas/src/components/terminal/TerminalWidget.tsx`
- `packages/canvas/src/components/CanvasContextMenu/index.tsx`
- `packages/canvas/src/components/FloatingCanvasToolbar/`
- `packages/canvas/src/components/CanvasHelp/index.tsx`
- `packages/canvas/src/components/SelectionStyleMenu/`
- `packages/canvas/src/new-plugins/transform/Transform.plugin.ts`

## Plan
### 1. Audit remaining hardcoded UI colors
Find remaining canvas-side hardcoded color values and raw Tailwind palette classes.

### 2. Theme terminal and overlays
Thread `ThemeService` or CSS vars into terminal, context menu, toolbar, help, style menu, and transformer visuals.

### 3. Keep explicit element colors working
Only use theme fallback colors when element data does not already specify a color.

## TODOS
- [x] List remaining canvas hardcoded colors and palette classes
- [x] Theme `GhosttyTerminalMount` colors from shared theme definitions
- [x] Theme `TerminalWidget` chrome and error states
- [x] Theme `CanvasContextMenu` surface and highlighted item states
- [x] Theme toolbar and help overlay states without hardcoded stone/amber classes
- [x] Theme selection style menu controls and focus/active states
- [x] Set transformer/anchor/border colors from theme instead of defaults
- [ ] Add regression checks for at least one canvas overlay and terminal theme path

## NOTES
- Keep canvas render behavior logic separate from theme lookup where possible
- Prefer CSS vars for DOM overlays and explicit color args for Konva nodes
- Do not break explicit per-element style colors when applying theme fallbacks

### LOGS
- agent: created from user request to split theme work into concrete tasks.
- agent: themed terminal chrome and Ghostty mount from shared terminal CSS vars.
- agent: replaced remaining toolbar/help/context-menu/style-menu stone/amber palette classes with semantic theme tokens.
- agent: themed transformer border/anchor visuals and transform drag proxy from shared theme colors.

---

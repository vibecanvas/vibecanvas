# A13 - canvas CLI: `ungroup` command

## Plan
- Add deterministic group removal for explicit group ids.
- Preserve absolute child positions after structure changes.
- Keep behavior aligned with runtime ungroup semantics.

## TODOS
- [ ] Write `packages/canvas/tests/cli/ungroup.test.ts` first, covering exact group ids, preserved child positions, and invalid non-group targets.
- [ ] Implement `vibecanvas canvas ungroup` as a document transform over explicit group ids.
- [ ] Extract or reuse pure ungroup helpers from the runtime/plugin path where possible.
- [ ] Return stable JSON describing removed group ids and released child ids.

## NOTES
- Category: addition.
- Scope decision: ungroup stays explicit and structural.
- Dependency: build after A5 and A6.

### LOGS
- Created from E8 after freezing the concrete implementation strategy.

---

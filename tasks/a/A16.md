# A16 - canvas CLI: `render` command

## Plan
- Ship rendering last because screenshot fidelity is the highest-risk CLI surface.
- Reuse the controlled canvas test harness where possible.
- Keep the contract explicit about viewport input and hosted-widget gaps.

## TODOS
- [ ] Write `packages/canvas/tests/cli/render.test.ts` first, covering isolated db bootstrap, explicit viewport/camera inputs, output file creation, and fidelity warnings.
- [ ] Implement `vibecanvas canvas render` using the headless stage path available to tests and offline runtime bootstrap.
- [ ] Document and emit warnings for DOM-hosted widgets and missing live-user viewport state.
- [ ] Keep the command explicitly experimental until the contract is stable.

## NOTES
- Category: addition.
- Scope decision: render lands last because DOM-widget screenshot fidelity is the riskiest part of v1.
- Dependency: build after A5 and A6 and after readonly/mutation commands stabilize.

### LOGS
- Created from E8 after freezing the concrete implementation strategy.

---

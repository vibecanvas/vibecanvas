# A17 - rect dbl click -> enter edit mode (inline text)

## Plan

Keep it small.
Make rect double click use same attached-text path as Enter.
Preserve drill-in behavior for grouped rects:
- first dblclick drills selection
- second dblclick opens inline text edit

Add focused tests.

## TODOS
- [x] Reuse attached-text open path for rect actions.
- [x] Wire rect `pointerdblclick` to create/open attached text edit when selection logic does not consume the dblclick.
- [x] Add test for top-level rect dblclick edit mode.
- [x] Add test for grouped rect drill-first, edit-second behavior.
- [ ] Verify test run once unrelated module resolution issue is fixed.

## NOTES

- `SelectPlugin` already consumes dblclick when it drills deeper into nested selection.
- Attached text stays rect-only here. No extra abstraction.
- Verification blocked by unrelated test env error:
  - `Cannot find module 'lucide-solid/icons/alert-triangle'`

### LOGS
- agent: read canvas shape/text selection flow.
- agent: added shared attached-text opener in `packages/canvas/src/plugins/Shape2d/Shape2d.drag.ts`.
- agent: rect dblclick now opens inline text edit when dblclick is not consumed by selection drill.
- agent: added tests in `packages/canvas/tests/plugins/shape2d/Shape2dPlugin.text.test.ts`.
- agent: tried `bun test packages/canvas/tests/plugins/shape2d/Shape2dPlugin.text.test.ts`; blocked by unrelated missing module from `ImageViewer.tsx`.

---

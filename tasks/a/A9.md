# A9 - canvas CLI: `query` command

## Plan
- Add structured scene queries without any natural-language parsing.
- Support explicit selectors so agents can ask for only the scene slice they need.
- Keep the command readonly and machine-friendly.

## TODOS
- [ ] Write `packages/canvas/tests/cli/query.test.ts` first, covering ids, types, group/subtree filters, bounds filters, zero/one/many matches, and default `summary` output.
- [ ] Implement `vibecanvas canvas query` with structured flags plus either `--where` or `--query`, rejecting ambiguous combinations.
- [ ] Return stable match metadata and scene payloads in `--json`.
- [ ] Keep the command strictly readonly with no natural-language mode.

## NOTES
- Category: addition.
- Scope decision: `query` stays structured and readonly in v1.
- Dependency: build after A5 and A6.

### LOGS
- Created from E8 after freezing the concrete implementation strategy.

---

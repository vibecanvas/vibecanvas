# A15 - canvas CLI: `reorder` command

## Plan
- Add explicit z-order operations for front/back/forward/backward moves.
- Keep semantics aligned with the existing render-order behavior.
- Make before/after order changes observable in tests and JSON output.

## TODOS
- [ ] Write `packages/canvas/tests/cli/reorder.test.ts` first, covering front/back/forward/backward operations on valid targets and invalid/no-op cases.
- [ ] Implement `vibecanvas canvas reorder` with stable action names and deterministic ordering behavior.
- [ ] Extract or reuse pure render-order helpers from the runtime/plugin path where possible.
- [ ] Return stable JSON for affected ids and before/after order data.

## NOTES
- Category: addition.
- Scope decision: reorder should match the current front/back vocabulary already used in the UI.
- Dependency: build after A5 and A6.

### LOGS
- Created from E8 after freezing the concrete implementation strategy.

---

# A10 - canvas CLI: `patch` command

## Plan
- Make small deterministic document edits via explicit structured patch payloads.
- Keep patches narrow and auditable.
- Use the command as the base mutation primitive before more semantic verbs land.

## TODOS
- [x] Write `packages/canvas/tests/cli/patch.test.ts` first, covering inline/file/stdin payloads, exact target selection, invalid payload errors, and changed-count JSON output.
- [x] Implement `vibecanvas canvas patch` with a stable structured patch envelope for element/group field updates.
- [x] Reject ambiguous selectors and invalid field/type combinations with testable errors.
- [x] Report exactly what changed in `--json`.

## NOTES
- Category: addition.
- Scope decision: patch is doc-first and deterministic, not a natural-language edit surface.
- Dependency: build after A5 and A6.

### LOGS
- Created from E8 after freezing the concrete implementation strategy.
- Added `packages/canvas-cmds/tests/cmd.patch.test.ts` covering explicit id patching, group patching, no-op changed counts, and invalid payload/type mismatch errors.
- Added `apps/cli/tests/cli/patch/cmd.patch.test.ts` covering inline, file, and stdin payload sources, exact id targeting, malformed payload failures, invalid field/type failures, and stable `changedCount` / `changedIds` JSON output.
- Implemented `packages/canvas-cmds/src/cmd.patch.ts` as an explicit id-based structured patch command with element/group payload branches, shallow `data` / `style` updates, group-cycle / missing-parent validation, and exact changed-id reporting.
- Implemented `apps/cli/src/canvas-cli/cmd.patch.ts` plus routing/help updates so `vibecanvas canvas patch` and top-level `vibecanvas patch` aliases work with `--patch`, `--patch-file`, and `--patch-stdin`.
- Updated `packages/api-canvas/src/contract.ts`, `packages/api-canvas/src/api.cmd.patch.ts`, and `packages/api-canvas/src/handlers.ts` so the shared ORPC canvas command surface also exposes `patch`.
- Verified with `bun test packages/canvas-cmds/tests apps/cli/tests/cli` and `bun -e "await import('./packages/api-canvas/src/handlers.ts'); await import('./packages/api-canvas/src/contract.ts'); console.log('ok')"`.

---

# A10 - canvas CLI: `patch` command

## Plan
- Make small deterministic document edits via explicit structured patch payloads.
- Keep patches narrow and auditable.
- Use the command as the base mutation primitive before more semantic verbs land.

## TODOS
- [ ] Write `packages/canvas/tests/cli/patch.test.ts` first, covering inline/file/stdin payloads, exact target selection, invalid payload errors, and changed-count JSON output.
- [ ] Implement `vibecanvas canvas patch` with a stable structured patch envelope for element/group field updates.
- [ ] Reject ambiguous selectors and invalid field/type combinations with testable errors.
- [ ] Report exactly what changed in `--json`.

## NOTES
- Category: addition.
- Scope decision: patch is doc-first and deterministic, not a natural-language edit surface.
- Dependency: build after A5 and A6.

### LOGS
- Created from E8 after freezing the concrete implementation strategy.

---

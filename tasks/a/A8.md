# A8 - canvas CLI: `inspect` command (removed)

## Plan
- This command was implemented briefly, then removed by product direction.
- Exact-id lookup should go through `vibecanvas canvas query --id <id>` instead of a dedicated `inspect` surface.
- Keep the history here so later agents understand why the command no longer exists in the CLI.

## TODOS
- [x] Document that the dedicated `inspect` command and its tests were removed.
- [x] Point exact-id read workflows at `canvas query --id <id>` instead.

## NOTES
- Category: addition.
- Scope decision: final naming is `inspect`, not `show`.
- Dependency: build after A5 and A6.
- Actual CLI e2e coverage lives in `apps/server/tests/cli/`, which is where the A6 harness landed.
- The dedicated `inspect` surface was removed after follow-up review; `query` is now the only readonly scene-selection command.
- Exact-id reads should use `vibecanvas canvas query --canvas <id> --id <target-id>` or the equivalent `--canvas-name` flow.

### LOGS
- Created from E8 after freezing the concrete implementation strategy.
- Removed `apps/server/src/canvas-cli/cmd.inspect.ts`, removed `apps/server/tests/cli/inspect/cmd.inspect.test.ts`, and deleted inspect-specific help wiring from `apps/server/src/canvas-cli/cmd.canvas.ts` plus `apps/server/tests/cli/harness.test.ts`.
- Exact-id inspection is now expected to flow through `canvas query --id <id>` rather than a dedicated subcommand.

---

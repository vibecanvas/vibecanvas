# A8 - canvas CLI: `inspect` command

## Plan
- Add exact-id inspection for one element or group inside one selected canvas.
- Keep the command readonly and precise.
- Reuse the shared output modes `summary`, `focused`, and `full`.

## TODOS
- [ ] Write `packages/canvas/tests/cli/inspect.test.ts` first, covering element lookup, group lookup, not-found errors, and ambiguous `--canvas-name` failures.
- [ ] Implement `vibecanvas canvas inspect <id>` with required `--canvas` or uniquely resolving `--canvas-name` selection.
- [ ] Define stable `summary`, `focused`, `full`, and `--json` contracts for a single inspected target.
- [ ] Fail clearly when the id does not exist inside the selected canvas.

## NOTES
- Category: addition.
- Scope decision: final naming is `inspect`, not `show`.
- Dependency: build after A5 and A6.

### LOGS
- Created from E8 after freezing the concrete implementation strategy.

---

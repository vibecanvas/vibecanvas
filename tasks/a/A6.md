# A6 - canvas CLI: end-to-end test harness

## Plan
- Start CLI work with TDD by creating a dedicated `packages/canvas/tests/cli/` harness.
- Give every test case a brand-new temp directory, db file, seeded canvas fixture, and CLI process invocation.
- Make the harness assert full end-to-end behavior, not direct helper calls.

## TODOS
- [x] Create `packages/canvas/tests/cli/` with shared helpers for temp dirs, temp db paths, cleanup, and real CLI process execution.
- [x] Add seeding helpers that create isolated canvas/doc/database fixtures per test without reusing developer-local storage.
- [x] Standardize assertions for exit code, stdout, stderr, and stable `--json` payloads.
- [x] Document the rule that every CLI test uses a fresh setup and passes `--db` explicitly.

## NOTES
- Category: addition.
- Source request: start with TDD, use a new `packages/canvas/tests/cli` folder, and require a new setup for every test.
- Dependency: follows A5 and supports A7-A16.
- A5 is now landed on this branch, so the harness validates both the top-level CLI entrypoint and the real `vibecanvas canvas ... --db <path>` path; command suites for A7-A16 can now reuse `runCanvasCli()` directly.
- Important test-runtime constraint: Vitest in this package cannot import `bun:sqlite` directly inside the test process, so the harness uses Node-side test orchestration plus a Bun worker subprocess for isolated DB bootstrap and Automerge fixture seeding.
- Important bootstrap constraint: the sandbox must be initialized through the real migration entrypoint (`packages/imperative-shell/src/database/migrate.ts`), not a hand-rolled schema stub, otherwise the offline canvas open path can fail later with migration drift such as `CANVAS_BOOTSTRAP_FAILED` during `ALTER TABLE filetrees DROP COLUMN x`.
- Harness setup now explicitly verifies that bootstrap reused `packages/imperative-shell/database-migrations` itself; helper workers no longer create fallback tables by hand.
- Existing A5 coverage already lives in `packages/canvas/tests/cli/db-path.test.ts`; the new A6 harness is designed to be the shared base that A7-A16 command suites can reuse instead of hand-rolling setup each time.
- Current limitation discovered while wiring the real canvas path: offline bootstrap still prints DB migration logs to stdout before command handling. The harness can exercise the path today, but truly clean machine-readable stdout for successful `--json` command output likely needs follow-up cleanup in the command implementation tasks.

### LOGS
- Created from E8 after freezing the concrete implementation strategy.
- Added `packages/canvas/tests/cli/harness.ts` with fresh temp sandbox creation, isolated SQLite migration/bootstrap, fixture seeding via real DB + Automerge storage, and subprocess runners for top-level CLI and future `canvas` commands.
- Added `packages/canvas/tests/cli/harness.test.ts` to prove every context gets a fresh migrated DB, seeded canvas docs persist to disk, subprocess assertions are standardized, and the real `apps/server/src/main.ts --help` entry point runs end to end.
- Added `packages/canvas/tests/cli/harness.worker.ts` so DB bootstrap and seeding run in a real Bun subprocess without introducing `src/` changes.
- Extended `packages/canvas/tests/cli/harness.test.ts` after A5 landed so the suite now also exercises the real `canvas list --json` path through `runCanvasCli()` and confirms the isolated `dbPath` is the one surfaced back by the offline command bootstrap.
- Switched sandbox initialization to call the real `packages/imperative-shell/src/database/migrate.ts` entrypoint after discovering that a simplified test-only schema caused `CANVAS_BOOTSTRAP_FAILED` on the real offline open path.
- Removed the worker-side fallback schema creation and tightened the harness so setup must come from the checked-in `packages/imperative-shell/database-migrations` folder.
- While extending the real canvas-path test, observed that offline state open still emits `[DB] Applying migrations ...` to stdout; recorded this as a follow-up constraint for A7+ JSON output expectations.
- Ran `bunx vitest --run packages/canvas/tests/cli/harness.test.ts` and fixed the harness until the new suite passed.
- Ran `bun --filter @vibecanvas/canvas test` and confirmed the full canvas package test suite passes with the new CLI harness included.

---

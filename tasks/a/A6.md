# A6 - canvas CLI: end-to-end test harness

## Plan
- Start CLI work with TDD by creating a dedicated `packages/canvas/tests/cli/` harness.
- Give every test case a brand-new temp directory, db file, seeded canvas fixture, and CLI process invocation.
- Make the harness assert full end-to-end behavior, not direct helper calls.

## TODOS
- [x] Create `packages/canvas/tests/cli/` with shared helpers for temp dirs, temp db paths, cleanup, and real CLI process execution.
- [x] Add seeding helpers that create isolated canvas/doc/database fixtures per test without reusing developer-local storage.
- [x] Standardize assertions for exit code, stdout, stderr, and stable `--json` payloads.
- [x] Document the rule that every CLI test uses a fresh setup and passes `--db` explicitly.

## NOTES
- Category: addition.
- Source request: start with TDD, use a new `packages/canvas/tests/cli` folder, and require a new setup for every test.
- Dependency: follows A5 and supports A7-A16.
- Current branch still does not expose the `canvas` subcommand yet, so the harness is validated with real subprocess smoke tests plus isolated DB/doc seeding, and `runCanvasCli()` is prepared for A7+ once A5 lands here.

### LOGS
- Created from E8 after freezing the concrete implementation strategy.
- Added `packages/canvas/tests/cli/harness.ts` with fresh temp sandbox creation, isolated SQLite migration/bootstrap, fixture seeding via real DB + Automerge storage, and subprocess runners for top-level CLI and future `canvas` commands.
- Added `packages/canvas/tests/cli/harness.test.ts` to prove every context gets a fresh migrated DB, seeded canvas docs persist to disk, subprocess assertions are standardized, and the real `apps/server/src/main.ts --help` entry point runs end to end.

---

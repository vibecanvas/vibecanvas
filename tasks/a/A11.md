# A11 - canvas CLI: `move` command

## Plan
- Add a semantic translate command for elements and group subtrees.
- Preserve relative positions for multi-node and group moves.
- Prefer extracted pure document helpers over CLI-only geometry rewrites.

## TODOS
- [ ] Write `packages/canvas/tests/cli/move.test.ts` first, covering single-element moves, multi-id moves, group-subtree translation, and stable changed-id JSON output.
- [ ] Implement `vibecanvas canvas move` as an explicit translate operation rather than a raw coordinate patch.
- [ ] Extract or reuse pure subtree-translation helpers from existing canvas logic where possible.
- [ ] Fail clearly on invalid targets and ambiguous selectors.

## NOTES
- Category: addition.
- Scope decision: group move is a subtree translation, not a stored group-position patch.
- Dependency: build after A5 and A6.

### LOGS
- Created from E8 after freezing the concrete implementation strategy.

---

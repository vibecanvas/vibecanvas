# A7 - canvas CLI: `list` command

## Plan
- Ship the discovery command first so tests and humans can inspect which canvases exist in a local db.
- Keep it serverless and deterministic.
- Establish the first stable human and `--json` output contract.

## TODOS
- [ ] Write `packages/canvas/tests/cli/list.test.ts` first, covering empty db, multiple canvases, deterministic ordering, and `--json` output.
- [ ] Implement `vibecanvas canvas list` against the explicit `--db` bootstrap without requiring `serve`.
- [ ] Define the stable output fields for canvas inventory entries.
- [ ] Keep selector/default behavior explicit and testable.

## NOTES
- Category: addition.
- Scope decision: `list` is the discovery command and may run without selecting one canvas first.
- Dependency: build after A5 and A6.

### LOGS
- Created from E8 after freezing the concrete implementation strategy.

---

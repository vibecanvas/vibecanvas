# A32 - hosted components: sandboxed Arrow runtime + per-component official packages
[Overview](../BASED.md)

Move hosted canvas widgets to sandboxed Arrow component bundles with builtin defaults and per-slot override support.

## Context
Current hosted UI is hardwired in canvas package:
- `packages/canvas/DESIGN.md`
- `packages/canvas/src/plugins/HostedSolidWidget/HostedSolidWidget.plugin.tsx`
- `packages/canvas/src/plugins/HostedSolidWidget/HostedSolidWidget.mount.tsx`
- `packages/canvas/src/plugins/IframeBrowserWidget/IframeBrowserWidget.plugin.tsx`
- `packages/canvas/src/plugins/ArrowJs/ArrowJs.plugin.ts`
- `packages/canvas/src/services/canvas/interface.ts`
- `packages/canvas/src/runtime.ts`
- `packages/service-automerge/src/types/canvas-doc.types.ts`

Need new architecture:
- trusted canvas host owns Konva node, drag, transform, focus, projection, and CRDT
- Arrow sandbox owns untrusted widget UI
- official widgets are not one shared package; each official widget gets its own package
- later user can load remote replacement without rebuilding app
- builtin official widgets still ship with app
- user can override slots like `filetree` with another implementation

Target package split:
- `packages/ui` -> `@vibecanvas/ui`
- `packages/hosted-component-filetree`
- `packages/hosted-component-file`
- `packages/hosted-component-terminal`
- `packages/hosted-component-iframe`

## Plan
### 1. Define `packages/ui` contract first
Write shared types for sandbox bundle format, manifest, bridge modules, and resolver shape in `@vibecanvas/ui`.

#### 1.1 Bundle format
Define self-contained source bundle shape for builtin and remote hosted UI packages.

#### 1.2 Manifest
Define id, version, apiVersion, slot, permissions, and default size metadata.

#### 1.3 Bridge types
Define typed host bridge modules for chrome, drop, and capability APIs.

#### 1.4 Resolver types
Define provider registration and slot resolution rules for builtin, user override, and per-element override.

### 2. Write canvas host architecture doc before implementation
Design trusted canvas host path on paper first so old hosted widget logic can collapse into one system.

#### 2.1 `ArrowJsService`
Own sandbox mount, unmount, remount, cache, and bridge wiring.

#### 2.2 `HostedComponentsService`
Own registry, slot resolution, builtin providers, overrides, and live swap notifications.

#### 2.3 `HostedComponentPlugin`
Own Konva host rects, projection, drag, transform, focus, z-order, and CRDT sync.

#### 2.4 Replacement map
Map old `HostedSolidWidgetPlugin` and `IframeBrowserWidgetPlugin` responsibilities into the new trusted host path.

### 3. Split official widgets into per-component packages
Make filetree, file, terminal, and iframe each ship as its own builtin package and register through the same resolver used for remote installs.

### 4. Plan remote install and live swap path
Define fetch, cache, validation, and remount flow so a running app can swap providers without rebuild.

## TODOS
- [x] Create `@vibecanvas/ui` task spec for hosted component bundle format
- [x] Create `@vibecanvas/ui` task spec for hosted component manifest
- [x] Create `@vibecanvas/ui` task spec for host bridge module types
- [x] Create `@vibecanvas/ui` task spec for provider and resolver types
- [x] Write canvas host architecture doc for `ArrowJsService`
- [x] Write canvas host architecture doc for `HostedComponentsService`
- [x] Write canvas host architecture doc for `HostedComponentPlugin`
- [x] Write replacement map from `HostedSolidWidgetPlugin` and `IframeBrowserWidgetPlugin` into new host path
- [ ] Split official widgets into one package each: filetree, file, terminal, iframe
- [ ] Decide CRDT or schema changes needed for provider ids or renderer overrides
- [ ] Define remote install, cache, validation, and live remount flow
- [ ] Pick migration order; terminal last unless proven simple

## NOTES
- User correction: official widgets must be separate packages, not one shared official package.
- User rename: shared hosted UI/sdk package should be `packages/ui` with package name `@vibecanvas/ui`.
- Arrow sandbox runs untrusted UI, but drag/drop and app API access still need trusted host bridges.
- Terminal likely needs special handling because the terminal surface may stay trusted host-side even if shell UI moves into sandbox.

### LOGS
- agent: created from user request after hosted-components architecture review.
- agent: captured correction that each official component must be its own package.
- agent: updated task naming from `packages/hosted-component-sdk` to `packages/ui` / `@vibecanvas/ui`.
- agent: reordered task to start with `@vibecanvas/ui` contract work, then canvas host architecture docs.
- agent: created `packages/ui` package skeleton with hosted component bundle, manifest, bridge, and resolver types.
- agent: wrote `packages/canvas/HOSTED_COMPONENTS_ARCHITECTURE.md` for `ArrowJsService`, `HostedComponentsService`, `HostedComponentPlugin`, and old-plugin replacement map.
- agent: updated `packages/canvas/DESIGN.md` to point at hosted component target architecture.

---

# B14 - canvas CLI: `canvas group --help` falls back to global help instead of subcommand help

## Plan
- Reproduce subcommand help routing for `group` and compare with implemented canvas subcommands.
- Trace how `--help` is handled in `apps/cli/src/plugins/cli/cmds/cmd.canvas.ts`.
- Route help to the command-specific handler or stop claiming subcommand help exists.

## TODOS
- [ ] Reproduce `canvas group --help` printing root help instead of group help.
- [ ] Check `cmd.canvas.ts` help dispatch for `group`, `ungroup`, `move`, `reorder`, `list`, and `query`.
- [ ] Make `canvas <subcommand> --help` consistently call the subcommand help printer.
- [ ] Add CLI coverage for command-specific help output.

## NOTES
- Category: bug.
- Found during real devserver probing via `apps/cli/src/main.ts`.
- Root help says any subcommand accepts `--help` for command-specific usage.
- `canvas group --help` currently prints the global CLI help instead.

### Repro
```bash
bun run apps/cli/src/main.ts canvas group --help
```

### Actual
- Prints the root `vibecanvas` help page.
- Does not print `group`-specific usage or options.

### Expected
- Print `group` command help with `--canvas`, `--canvas-name`, `--id`, `--json`, and examples/notes.

### LOGS
- Added from real dev divergent experiment token `dff406a4`.
- Confirmed against live devserver with the CLI routed through `apps/cli/src/main.ts`.
- This likely affects other canvas subcommand help paths, not just `group`.

---

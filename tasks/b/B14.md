# B14 - canvas CLI: `group` and `ungroup` still return not implemented and fall back to generic help in `apps/cli/src/main.ts`

## Plan
- Reproduce the current `apps/cli/src/main.ts` behavior for `canvas group` and `canvas ungroup` against the real dev database.
- Check whether the commands exist elsewhere but are not routed/wired through the active CLI entrypoint.
- Make sure command-specific help resolves once the commands are actually available.

## TODOS
- [ ] Reproduce `canvas group` and `canvas ungroup` failures through `bun run apps/cli/src/main.ts ...`.
- [ ] Trace whether the implementations live in another path/package but are not connected in the active CLI router.
- [ ] Wire `group` / `ungroup` through the same shared command path as `query` / `patch` / `move` / `delete`.
- [ ] Add real help coverage so `vibecanvas canvas group --help`, `vibecanvas canvas ungroup --help`, and top-level aliases stop falling back to generic canvas help.

## NOTES
- Category: bug.
- Reproduced from the real CLI entrypoint requested for experimentation: `apps/cli/src/main.ts`.
- Current behavior:
  - `bun run apps/cli/src/main.ts canvas group --canvas-name hello --id ... --json`
  - `bun run apps/cli/src/main.ts canvas ungroup --canvas-name hello --id ... --json`
  - both return `CANVAS_COMMAND_NOT_IMPLEMENTED`.
- `canvas group --help` and `canvas ungroup --help` currently fall back to the generic `canvas` help text instead of command-specific usage.
- This conflicts with the expectation that group/ungroup are now implemented.

### LOGS
- Reproduced with:
  - `bun run apps/cli/src/main.ts canvas group --canvas-name hello --id cf36ae0d-2f96-489f-aaaa-f3549c5ef366 --id a70655ba-e16f-4525-ae25-2bbf8d3e452c --json`
  - `bun run apps/cli/src/main.ts canvas ungroup --canvas-name hello --id 87bccbb2-30d8-4dec-8f04-195a857309d0 --json`
- Both commands exited with:
  - `{ "ok": false, "command": "canvas", "code": "CANVAS_COMMAND_NOT_IMPLEMENTED", ... }`

---

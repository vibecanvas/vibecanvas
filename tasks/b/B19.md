# B19 - cli usage -> no realtime updates via automerge

## Plan
- Reproduce expected path mentally from CLI mutation to frontend Automerge sync.
- Check whether CLI writes go through the running server or a separate local repo.
- Check whether frontend canvas scene subscribes to Automerge handle updates after initial load.
- Capture likely root cause and next fix steps.

## TODOS
- [x] Confirm CLI prefers local server when `--db` is not passed.
- [x] Confirm canvas mutation commands edit the server-side Automerge doc handle.
- [x] Inspect frontend canvas hydration path for live Automerge subscriptions.
- [ ] Add a live doc change subscription in canvas runtime and rehydrate/update scene on remote changes.
- [ ] Add regression coverage: mutate canvas via CLI while canvas page is open; assert shape appears without refresh.

## NOTES
- User report: with dev server on port 3000, running `vibecanvas canvas add ...` succeeds but frontend does not show the new shape until page refresh.
- `apps/cli/src/plugins/cli/core/fx.dispatch-canvas-command.ts` confirms CLI tries local API server first when `--db` is not passed.
- So with the server running, CLI mutation likely executes inside the server process via RPC, not in a detached local-only repo.
- `packages/canvas-cmds/src/core/fx.canvas.ts` loads the canvas doc from `portal.automergeService.repo.find(...)`, waits for readiness, then mutation commands call `handle.change(...)` on that handle.
- This means the write path is hitting the server Automerge repo correctly.
- Frontend does connect to Automerge websocket at `/automerge` via `apps/frontend/src/services/automerge.ts`.
- But canvas runtime only hydrates scene once on init.
- `packages/canvas/src/components/Canvas.tsx` loads one `DocHandle` and creates `CanvasService` once per handle.
- `packages/canvas/src/plugins/SceneHydrator/SceneHydrator.plugin.ts` only runs `loadCanvas()` from `context.hooks.initAsync`.
- No listener was found for `docHandle` change / update events that would re-run hydration or incrementally patch the scene.
- So refresh works because page reload rebuilds `CanvasService`, which re-runs one-time hydration from the latest doc snapshot.
- Most likely root cause: frontend scene is not subscribed to live Automerge doc updates after initial hydration.

### LOGS
- Read CLI dispatch path and confirmed server-first behavior without `--db`.
- Read canvas command core and confirmed mutations call `handle.change(...)` on the server Automerge repo handle.
- Read frontend `Canvas.tsx`, `Canvas.service.ts`, and `SceneHydrator.plugin.ts`.
- Found one-time hydration only, with no live Automerge change subscription in the canvas scene path.

---

# B13 - canvas CLI: top-level alias docs lie; `vibecanvas query ...` is rejected

## Plan
- Reproduce the advertised top-level alias flow against the real dev CLI.
- Trace argv parsing and command dispatch for canvas subcommand aliases.
- Align help text with real behavior or implement the alias routing.

## TODOS
- [ ] Reproduce `vibecanvas query ...` failing while root help claims the alias works.
- [ ] Trace `apps/cli/src/parse-argv.ts` + `plugins/cli/CliPlugin.ts` dispatch for unknown commands.
- [ ] Either route top-level canvas aliases into `canvas <subcommand>` or remove the alias claim from help.
- [ ] Add CLI coverage for at least one top-level alias success/failure contract.

## NOTES
- Category: bug.
- Found during real devserver probing via `apps/cli/src/main.ts`.
- Root help currently claims:
  - `vibecanvas query --help`
  - `Canvas subcommands also work as top-level aliases`
- Actual behavior rejects the alias as an unknown command.

### Repro
```bash
bun run apps/cli/src/main.ts query --canvas-name recorder --id d7a22c65-f7f4-41c2-877e-ca9eb1946fba --json
```

### Actual
- Exits non-zero.
- Prints `Unknown command: query`.
- Then prints root help that still claims the alias works.

### Expected
- Either:
  - route to `canvas query ...`, or
  - stop advertising the alias in root help.

### LOGS
- Added from real dev divergent experiment token `dff406a4`.
- Confirmed against live devserver with `apps/server/src/main.ts serve` running.
- Observed mismatch between documented alias behavior and actual dispatch.

---

# B15 - canvas CLI: `--json` output is not real JSON; commands print Bun-inspected objects

## Plan
- Reproduce the JSON contract failure on real canvas commands.
- Trace command result printing in `apps/cli/src/plugins/cli/cmds/*.ts`.
- Make `--json` use `JSON.stringify(...)` consistently for success and error output.

## TODOS
- [ ] Reproduce `--json` output containing trailing commas / `[Object ...]` placeholders.
- [ ] Audit `cmd.query.canvas.ts`, `cmd.list.canvas.ts`, `cmd.move.canvas.ts`, `cmd.group.canvas.ts`, `cmd.ungroup.canvas.ts`, and `cmd.reorder.canvas.ts` result printing.
- [ ] Switch success-path JSON output to `JSON.stringify(result)` when `config.subcommandOptions?.json` is true.
- [ ] Add CLI tests that parse stdout as JSON for implemented commands.

## NOTES
- Category: bug.
- Found during real devserver probing via `apps/cli/src/main.ts`.
- Multiple commands accept `--json`, but current success output uses `console.log(result)`.
- Under Bun this prints inspect-style objects, not strict JSON.

### Repro
```bash
bun run apps/cli/src/main.ts canvas query --canvas-name recorder --id d7a22c65-f7f4-41c2-877e-ca9eb1946fba --output full --json
```

### Actual
- Output contains Bun-inspect formatting like trailing commas and `[Object ...]`.
- Output is not machine-parseable JSON.

### Expected
- Strict JSON string on stdout for all `--json` success paths.

### LOGS
- Added from real dev divergent experiment token `dff406a4`.
- Confirmed on real `query`, `move`, `group`, `ungroup`, and `reorder` command paths.
- This is a contract bug, not only a formatting nit, because scripts cannot reliably parse the output.

---

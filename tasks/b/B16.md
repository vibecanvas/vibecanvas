# B16 - canvas CLI eagerly imports missing websocket adapter and crashes before any canvas command/help runs

## Plan
- Confirm the old bootstrap failure no longer reproduces on the current `apps/cli/src/main.ts` surface.
- Record the code-path change that made canvas help/list lazy enough to avoid the old eager websocket import crash.

## TODOS
- [x] Re-test `canvas list`, `canvas --help`, and top-level canvas aliases like `query --help`.
- [x] Verify the websocket adapter is no longer eagerly imported on help/list bootstrap paths.
- [x] Close this bug and leave any remaining live/offline document-availability issues to separate tasks.

## NOTES
- Category: bug.
- Status: closed.
- Re-tested on the current tree and the old crash no longer reproduces.
- These now work:
  - `bun run apps/cli/src/main.ts canvas list --json`
  - `bun run apps/cli/src/main.ts canvas --help`
  - `bun run apps/cli/src/main.ts query --help`
  - `bun run apps/cli/src/main.ts move --help`
- `canvas list --json` returned a valid inventory instead of crashing.
- The current CLI path now lazy-loads canvas state/client modules through `apps/cli/src/plugins/cli/cmd.canvas.ts` and `apps/cli/src/plugins/cli/canvas.shared.ts`.
- The current `apps/cli/src/plugins/cli/canvas.local-state.ts` does not eagerly import `BrowserWebSocketClientAdapter`.
- There is still a separate live/offline issue where some no-`--db` query flows can fail with document-unavailable errors, but that is not this missing-module bootstrap crash.

### LOGS
- Verified success with:
  - `bun run apps/cli/src/main.ts canvas list --json`
  - `bun run apps/cli/src/main.ts canvas --help`
  - `bun run apps/cli/src/main.ts query --help`
  - `bun run apps/cli/src/main.ts move --help`
- Read:
  - `apps/cli/src/plugins/cli/cmd.canvas.ts`
  - `apps/cli/src/plugins/cli/canvas.shared.ts`
  - `apps/cli/src/plugins/cli/canvas.local-state.ts`

---

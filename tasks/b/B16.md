# B16 - canvas CLI eagerly imports missing websocket adapter and crashes before any canvas command/help runs

## Plan
- Reproduce the current `apps/cli/src/main.ts` canvas command bootstrap failure.
- Confirm that even help-only canvas paths fail before command routing because `local-state.ts` eagerly imports the live websocket adapter.
- Decide whether the live fallback dependency should be installed, lazily imported, or made optional behind runtime checks.

## TODOS
- [ ] Reproduce the fatal missing-module failure for `canvas list`, `canvas --help`, and top-level canvas aliases like `query --help`.
- [ ] Trace why help-only flows import `apps/cli/src/canvas-cli/local-state.ts` before they need any DB/doc state.
- [ ] Confirm whether `@automerge/automerge-repo-network-websocket` is intentionally absent from the current dependency graph or just missing from installation.
- [ ] Fix the bootstrap path so canvas help and local/offline commands do not hard-crash when the optional live fallback adapter is unavailable.

## NOTES
- Category: bug.
- Found during the second round of real-dev CLI experimentation.
- Current fatal error:
  - `Cannot find module '@automerge/automerge-repo-network-websocket' from '/Users/omarezzat/Workspace/vibecanvas/vibecanvas/apps/cli/src/canvas-cli/local-state.ts'`
- This currently blocks:
  - `bun run apps/cli/src/main.ts canvas list --json`
  - `bun run apps/cli/src/main.ts canvas --help`
  - `bun run apps/cli/src/main.ts query --help`
  - `bun run apps/cli/src/main.ts move --help`
  - `bun run apps/cli/src/main.ts patch --help`
  - `bun run apps/cli/src/main.ts delete --help`
  - `bun run apps/cli/src/main.ts group --help`
  - `bun run apps/cli/src/main.ts ungroup --help`
- Top-level non-canvas help still works: `bun run apps/cli/src/main.ts --help`.
- `apps/cli/src/canvas-cli/local-state.ts` now eagerly imports `BrowserWebSocketClientAdapter` at module load time, so even command/help paths that should stay local/offline can crash before routing.

### LOGS
- Reproduced with:
  - `bun run apps/cli/src/main.ts canvas list --json`
  - `bun run apps/cli/src/main.ts canvas --help`
  - `bun run apps/cli/src/main.ts query --help`
  - `bun run apps/cli/src/main.ts delete --help`
- Read `apps/cli/src/canvas-cli/local-state.ts` and confirmed the top-level import:
  - `import { BrowserWebSocketClientAdapter } from '@automerge/automerge-repo-network-websocket';`

---

# B16 - ci: @vibecanvas/canvas tests resolve Konva node build and require native `canvas`

## Plan
- reproduce or inspect CI-only failure path
- compare resolver/toolchain behavior against last known good commit
- force browser Konva entry for canvas tests or otherwise stabilize resolution across Linux CI
- re-enable `@vibecanvas/canvas` in root CI test filter

## TODOS
- [ ] reproduce the failure in Linux-like clean env
- [ ] confirm whether Vitest/Vite version drift changed Konva resolution
- [ ] patch `packages/canvas/vitest.config.ts` to force browser-safe Konva resolution
- [ ] restore `@vibecanvas/canvas` in root `test` script after fix

## NOTES
CI currently fails in `@vibecanvas/canvas` with:
- `Cannot find module 'canvas'`
- require stack through `konva/lib/index-node.js`

`packages/canvas/vitest.config.ts` did not change from the last known good commit.
Most likely cause is resolver/toolchain drift from updated lockfile versions picking Konva's node entry in CI.

Temporary mitigation: exclude `@vibecanvas/canvas` from root parallel test run by using explicit positive workspace filters for the other test packages.

### LOGS
- inspected diff against `6cd12d8a18c73e0a789dd306c88bd6eae9b975f6`
- confirmed `packages/canvas/vitest.config.ts` is unchanged
- identified likely cause as lockfile/toolchain drift plus CI env difference
- temporarily excluding `@vibecanvas/canvas` from root CI test fanout

---

# Git Worktree

The user will tell you to read this file when you are running this repo `vibecanvas` in a worktree.

On merge you need to merge this worktree back to the base branch.
Afterwards this worktree is stale. Don't delete. User will manage deletion.